import React from "react";
import App from "./App";
import { createRoot } from "react-dom/client";
import ConfigureRoutes from "./ConfigureRoutes";

createRoot(document.getElementById("root")).render(<ConfigureRoutes />);
